# Historical outputs

`outputs/verdict.json` is retained for compatibility with the earlier small proxy checks. The canonical current release is under `evidence/faithful/` and `.openresearch/artifacts/`, with repository-level interpretation in `publication_gate.json`.

The generated evidence uses the literal verdict `VERIFIED` for a passing finite contract. Read it together with the scoped status in the root README and audit report; it does not mean that a finite LP has replaced a universal theorem proof.

Use these files as the current sources of truth:

- `evidence/faithful/summary.json`
- `evidence/faithful/claim_1/` through `claim_6/`
- `.openresearch/artifacts/summary.json`
- `release/awaiting-judge.json`
- `publication_gate.json`
